@model IEnumerable<examenFinal.Cotizacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.buque)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.viaje)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.manifiesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.buque)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.viaje)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.manifiesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCotizacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCotizacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCotizacion })
        </td>
    </tr>
}

</table>
