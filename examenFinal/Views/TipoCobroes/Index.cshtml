@model IEnumerable<examenFinal.TipoCobro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.referencia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.referencia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTipoCobro }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTipoCobro }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTipoCobro })
        </td>
    </tr>
}

</table>
