@model IEnumerable<examenFinal.CotizacionDetalleCobro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CotizacionCobro.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tarifa.referencia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CotizacionCobro.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tarifa.referencia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCotizacionDetalleCobro }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCotizacionDetalleCobro }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCotizacionDetalleCobro })
        </td>
    </tr>
}

</table>
